<?php

// DBハンドル取得
$dbh = XXXX

// error logをいったん空っぽにする

// tableをいったん、trancate

// CSVファイルから情報を取得
$file_path = __DIR__ . '\\csv_date\\';
$dir = new DirectoryIterator($file_path);
foreach() {
	// １レコードを取得

	// あらかじめデータを一部修正

	// validate
	if (false) {
		error logに吐く
			ファイル名
			エラー箇所
	}

	// productテーブルにinsert
	// memberテーブルにinsert
}

